---
phase: 03-pdf-polish
plan: 03-03
type: standard
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/services/pdf-generator.ts
  - src/lib/services/pdf-helpers.ts
autonomous: true
---

<objective>
Add brand voice, do/don't cards, mockup gallery, and back cover sections to complete the PDF brand guide.

Purpose: Final sections that round out the brand guide with practical usage rules and real-world applications.
Output: Complete 12-section PDF brand guide ready for customer delivery.
</objective>

<context>
@.planning/phases/03-pdf-polish/03-CONTEXT.md
@.planning/PROJECT.md
@docs/PDF_REQUIREMENTS_TEMPLATE.md
</context>

<requirements>
- PDF-06: PDF includes embedded photo-realistic mockup images
</requirements>

<decisions>
**Do/don't cards (LOCKED):**
- Visual examples in grid layout
- 5+ Do examples, 4 Don't examples

**Mockup gallery (LOCKED):**
- Full-width per mockup
- Canvas + API mockups included
- Detailed captions

**Sections (LOCKED):**
- Brand voice with "We are / We are NOT" matrix
- Mockup gallery with all available mockups
- Back cover with branded design
</decisions>

<tasks>

<task name="Add brand voice section with we-are matrix">
Add brand voice section:

From wireframe (PAGE 10):
- "Brand Voice & Tone" header
- "We are / We are NOT" comparison table:
  - We are: Confident, Warm, Expert
  - We are NOT: Arrogant, Casual, Pretentious
- Voice guidelines and messaging examples

Data source: `sections.brandVoice`, `sections.visualStyleGuide`

Implementation:
- Draw two-column comparison box
- Left column: "We are" traits
- Right column: "We are NOT" traits
- Below: additional voice guidelines text

<verify>Brand voice section shows we-are matrix</verify>
<done>Brand voice section with comparison matrix</done>
</task>

<task name="Add do/don't cards in grid layout">
Add visual do/don't cards section:

From wireframe + decisions:
- Grid of mini cards showing correct and incorrect logo usage
- 5+ "Do" examples: solid background, clear space, approved colors, proper scaling, dark/light variants
- 4 "Don't" examples: stretch, rotate, recolor, add effects
- Each card: small logo image + label

Implementation:
- Create a helper function `drawDoDontCard(pdf, x, y, width, height, logoBase64, label, isDo)`
- Draw grid layout with cards
- Green checkmark for Do's, red X for Don'ts
- Use logo image (scaled down) in each card

Note: For visual examples, create variations of the logo programmatically:
- Do: normal logo, logo on dark bg, logo with clear space, small logo, inverted colors
- Don't: stretched logo, rotated logo, wrong colors, with effects overlay

<verify>Do/don't cards render in grid with visual examples</verify>
<done>Do/don't section with visual card grid</done>
</task>

<task name="Upgrade mockup gallery with full-width display">
Upgrade mockup gallery section:

From wireframe (PAGE 11-12):
- Each mockup gets full-width display
- Detailed caption with use case description
- Include all available mockups: canvas (business card, letterhead, t-shirt) + API mockups if available

Data source: `mockups.businesscard`, `mockups.letterhead`, `mockups.tshirt` (base64 images)

Implementation:
- For each available mockup:
  - New page or section
  - Full-width image (scaled to fit within margins)
  - Detailed caption below (e.g., "Professional networking — Business card with logo on premium cardstock")
- API mockups: check if any exist in order data, include if available

Captions by mockup type:
- Business card: "Professional networking — First impressions that last"
- Letterhead: "Official correspondence — Branded stationery for formal communication"
- T-shirt: "Branded merchandise — Wearable brand identity"
- API mockups: Descriptive caption based on product type

<verify>Mockup gallery shows all available mockups with captions</verify>
<done>Mockup gallery with full-width mockups and detailed captions</done>
</task>

<task name="Add branded back cover">
Add back cover page:

From wireframe (PAGE 13):
- Full brand color fill (matching cover)
- Business name centered
- Tagline below
- Version and date
- Contact email

Implementation:
- Mirror the cover page style (full color fill)
- Simpler layout — just name, tagline, version, contact
- No page number footer on back cover

<verify>Back cover shows branded design with contact info</verify>
<done>Back cover with branded design</done>
</task>

<task name="Update PDF data transformer for new sections">
Update `src/lib/services/pdf-data-transformer.ts` to ensure all new sections receive proper data:

- Verify `sections.projectOverview` is populated (brand story)
- Verify `sections.brandIdentity` is populated
- Verify `sections.imageryStyle` is populated
- Verify `sections.brandVoice` is populated
- Verify `sections.logoPhilosophy` is populated
- Ensure mockup data is passed through correctly

Add fallbacks for missing data (use placeholder text if AI hasn't generated content).

<verify>Data transformer provides all required fields for 12 sections</verify>
<done>Data transformer updated with fallbacks for new sections</done>
</task>

</tasks>

<verification>
- Brand voice shows we-are/not matrix
- Do/don't cards render in grid with visual examples
- Mockup gallery shows all available mockups with captions
- Back cover matches cover styling
- All 12 sections render in order
- PDF generates without errors
</verification>

<must_haves>
- [ ] Brand voice section with comparison matrix
- [ ] Do/don't cards with visual examples in grid
- [ ] Mockup gallery with full-width images and captions
- [ ] Back cover with branded design
- [ ] All 12 sections present and in order
  </must_haves>
