---
phase: 03-pdf-polish
plan: 03-02
type: standard
wave: 2
depends_on:
  - 03-01
files_modified:
  - templates/pdf/pages/colors.hbs
  - templates/pdf/pages/typography.hbs
  - templates/pdf/pages/logo-showcase.hbs
  - src/lib/services/pdf-helpers.ts
autonomous: true
---

<objective>
Polish the color palette, typography, and logo showcase page templates to achieve agency-quality visual design.

Purpose: These are the core visual identity sections. They need to look like they came from Pentagram or Collins.
Output: Beautiful color swatches, font specimens, and logo presentation.
</objective>

<context>
@.planning/phases/03-pdf-polish/03-CONTEXT.md
@.planning/PROJECT.md
@docs/PDF_REQUIREMENTS_TEMPLATE.md
</context>

<requirements>
- PDF-03: PDF has color palette section with hex/RGB values and swatches
- PDF-04: PDF has typography section with font specimens
- PDF-05: PDF has logo usage guidelines (minimum size, clear space, do/don't)
</requirements>

<tasks>

<task name="Polish color palette template">
Update `templates/pdf/pages/colors.hbs`:

Current state: Already has primary/secondary/accent swatches with HEX/RGB/CMYK, 60/30/10 usage bar.

Improvements needed:
- Larger swatches for primary colors (more visual impact)
- Better value label typography (monospace for hex, clean for RGB/CMYK)
- Visual hierarchy between primary (large), secondary (medium), accent (small)
- Add a color accessibility note section
- Improve the usage bar visual (rounded corners per pack design tokens)
- Add a "Color Combinations" section showing approved pairings

<verify>Color palette shows large swatches with all values and usage guidelines</verify>
<done>Color palette template polished</done>
</task>

<task name="Polish typography template">
Update `templates/pdf/pages/typography.hbs`:

Current state: Shows font names and basic hierarchy.

Improvements needed:
- Larger font specimen samples (the actual font rendered at display sizes)
- Type scale visualization: H1 (36pt), H2 (24pt), H3 (18pt), Body (12pt), Caption (9pt)
- Character set showcase (Aa Bb Cc Dd 1234567890)
- Body text sample paragraph at actual reading size
- Note about custom font availability (Google Fonts loaded in main.hbs)

The heading font and body font are selected per archetype pack (e.g., Anton + Roboto Condensed for bold-intense). The Google Fonts import in main.hbs already loads them.

<verify>Typography shows font specimens at multiple sizes with character sets</verify>
<done>Typography template polished with specimens</done>
</task>

<task name="Polish logo showcase template">
Update `templates/pdf/pages/logo-showcase.hbs`:

Current: Basic logo display.

Improvements needed:
- Full-page hero treatment — logo large and centered on white/light background
- Generous whitespace around the logo
- Logo philosophy text below (from `sections.logoPhilosophy`)
- Subtle brand color accent (thin line or corner element)

The logo is rendered as inline SVG (from `logoSvg` variable) or as an image. Ensure it scales properly.

<verify>Logo showcase is full-page with hero treatment and philosophy text</verify>
<done>Logo showcase template polished</done>
</task>

<task name="Polish do/don't template with visual cards">
Update `templates/pdf/pages/dos-donts.hbs`:

Current state: Text lists with 2 visual misuse examples (stretched, rotated).

Improvements needed (per CONTEXT.md decisions):
- Grid of mini cards (not just text lists)
- 5+ Do examples with visual demonstration
- 4 Don't examples with visual demonstration
- Green/red color coding
- Each card: visual example + label

Visual examples to create (using CSS transforms on the logo text):
- Do: on solid bg, with clear space, approved colors, small size, inverted
- Don't: stretched, rotated, wrong colors, with effects

Use CSS Grid for the card layout. Each card has a border, rounded corners (per pack design), and a label.

<verify>Do/don't section shows visual card grid with examples</verify>
<done>Do/don't template upgraded to visual card grid</done>
</task>

</tasks>

<verification>
- Color palette has large swatches with HEX/RGB/CMYK values
- Typography shows font specimens at multiple sizes
- Logo showcase is full-page hero treatment
- Do/don't cards render in visual grid layout
- All templates use design tokens from main.hbs
</verification>

<must_haves>
- [ ] Color palette shows swatches with all color values
- [ ] Typography shows font specimens and type hierarchy
- [ ] Logo showcase is full-page with philosophy text
- [ ] Do/don't cards show visual examples in grid
  </must_haves>
