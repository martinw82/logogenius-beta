---
phase: 03-pdf-polish
plan: 03-02
type: standard
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/services/pdf-generator.ts
autonomous: true
---

<objective>
Add logo guidelines, color palette, typography, and imagery style sections to the PDF brand guide.

Purpose: These are the core brand identity sections that define how the brand looks and feels visually.
Output: Professional logo usage rules, color swatches with values, font specimens, and imagery guidelines.
</objective>

<context>
@.planning/phases/03-pdf-polish/03-CONTEXT.md
@.planning/PROJECT.md
@docs/PDF_REQUIREMENTS_TEMPLATE.md
</context>

<requirements>
- PDF-04: PDF has typography section with font specimens
- PDF-05: PDF has logo usage guidelines (minimum size, clear space, do/don't)
- PDF-06: PDF has color palette section with hex/RGB values and swatches
</requirements>

<decisions>
**Do/don't cards (LOCKED):**
- Visual examples (not text-only)
- 5+ "Do" examples
- 4 "Don't" examples
- Grid of mini cards layout
</decisions>

<tasks>

<task name="Add logo guidelines section with clear space diagram">
Add logo guidelines section after logo showcase:

From wireframe (PAGE 6):
- Clear space diagram: dotted border around logo showing required clearance
- "Maintain 1x height clearance" label
- Minimum size rules: Print 25mm | Digital 80px
- Data source: Logo image for diagram, `sections.usageRulesAndDonts`

Implementation:
- Draw dotted rectangle around logo image to show clear space
- Add text labels for minimum sizes
- Use jsPDF drawing functions for the diagram

<verify>Logo guidelines show clear space diagram and minimum sizes</verify>
<done>Logo guidelines section with clear space and minimum size rules</done>
</task>

<task name="Upgrade color palette with computed values">
Upgrade color palette section with full color information:

From wireframe (PAGE 7):
- Primary colors: large swatches with HEX, RGB, CMYK values
- Secondary colors: medium swatches
- Accent colors: small swatches
- Usage ratio: "60% Primary / 30% Secondary / 10% Accent"

Data source: `sections.colorPalette.{primary, secondary, accent}` (arrays of hex strings)

Implementation:
- Existing `hexToRgb()` and `rgbToCmyk()` helpers already compute these values
- Draw colored rectangles as swatches
- Print values below each swatch
- Larger swatches for primary, smaller for secondary/accent

<verify>Color palette shows swatches with HEX, RGB, CMYK values</verify>
<done>Color palette section with computed RGB/CMYK values</done>
</task>

<task name="Upgrade typography with font specimens">
Upgrade typography section with actual font rendering:

From wireframe (PAGE 8):
- "Selected Fonts" header
- Headings font: name + sample text at large size
- Body font: name + sample paragraph
- Type hierarchy: H1 (36pt), H2 (24pt), H3 (18pt), Body (12pt), Caption (9pt)

Data source: `fonts.headings.name`, `fonts.body.name`, `sections.typography.*`

Note: jsPDF only supports built-in fonts (helvetica, times, courier) without custom font embedding. For now, show font names and hierarchy using available fonts. Custom font embedding is a future enhancement.

Implementation:
- List font names prominently
- Show hierarchy with size examples using helvetica
- Include typography usage guidelines text

<verify>Typography section shows font names and hierarchy examples</verify>
<done>Typography section with font specimens and hierarchy</done>
</task>

<task name="Add imagery style section">
Add new imagery style section:

From wireframe (PAGE 9):
- "Imagery & Photography Style" header
- Guidelines for visual content:
  - Photography mood/tone
  - Composition rules
  - Color treatment
  - Illustration style

Data source: `sections.imageryStyle`, `sections.graphicElements`

Implementation:
- Section header with brand accent bar
- Body text from AI-generated imagery guidelines
- Styled as editorial text block

<verify>Imagery style section renders with guidelines</verify>
<done>Imagery style section added</done>
</task>

</tasks>

<verification>
- Logo guidelines with clear space diagram
- Color palette with HEX/RGB/CMYK swatches
- Typography with font names and hierarchy
- Imagery style section with guidelines
- All sections follow brand styling
</verification>

<must_haves>
- [ ] Logo guidelines show clear space rules
- [ ] Color palette has swatches with HEX, RGB, CMYK values
- [ ] Typography shows font specimens and hierarchy
- [ ] Imagery style section with guidelines
  </must_haves>
