---
phase: 05-social-quality
plan: 05-02
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/hooks/useClientSocialGenerator.ts
  - src/app/admin/orders/[id]/page.tsx
requirements:
  - SOC-02
  - SOC-03
autonomous: true
---

<objective>
Add brand font loading infrastructure, three-tier typography hierarchy, frosted glass containers, and proportional text sizing across all 7 Canvas social templates. Ensure design consistency through shared visual language.

Purpose: Transform system-font text into polished brand typography with clear hierarchy and consistent design treatment across all platforms.
</objective>

<context>
@.planning/phases/05-social-quality/05-CONTEXT.md
@.planning/phases/05-social-quality/05-RESEARCH.md
@.planning/phases/05-social-quality/05-01-SUMMARY.md
</context>

<verification>
All 7 templates render with brand fonts, three-tier text hierarchy, frosted containers, and proportional sizing. Design language is consistent across platforms.
</verification>

<tasks>

<task>
<name>Wire font data from order to social generator</name>
<read_first>
- src/app/admin/orders/[id]/page.tsx (lines 360-380)
- src/lib/services/font-loader.ts (existing loadFonts function)
</read_first>
<action>
1. Read the existing `loadFonts()` function in `src/lib/services/font-loader.ts` — it already loads Google Fonts from font names
2. Read `src/app/admin/orders/[id]/page.tsx` around line 367-377 where `generateAndUploadSocialAssets()` is called
3. Extend the call to pass `fontHeadings: order.data.fontHeadings` and `fontBody: order.data.fontBody` in the options object
4. Verify the order data type includes these fields (check Prisma schema or order types)
</action>
<acceptance_criteria>
- page.tsx passes fontHeadings and fontBody from order data to generateAndUploadSocialAssets
- Order data type confirmed to have fontHeadings and fontBody fields
</acceptance_criteria>
</task>

<task>
<name>Add font loading infrastructure</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 1-100)
- src/lib/services/font-loader.ts (existing loadFonts function)
</read_first>
<action>
1. Extend `SocialAssetOptions` interface to add optional fields:
   - `fontHeadings?: string`
   - `fontBody?: string`
2. Reuse the existing `loadFonts()` pattern from `src/lib/services/font-loader.ts` — adapt for client-side Canvas use:
   - Construct Google Fonts CSS URL from font names
   - Append `<link>` to document.head if not already present
   - Await `document.fonts.ready` promise
   - Fall back gracefully to sans-serif if font fails
3. Create `getFont(family: string | undefined, fallback: string)` helper:
   - Returns loaded font family or fallback
4. Call font loading once at the start of `generateAndUploadSocialAssets()` before any template rendering
</action>
<acceptance_criteria>
- SocialAssetOptions has fontHeadings and fontBody optional fields
- Font loading reuses pattern from existing font-loader.ts
- Fonts loaded once before template rendering
- Fallback to sans-serif if font fails
</acceptance_criteria>
</task>

<task>
<name>Create typography helper functions</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 93-199)
</read_first>
<action>
1. Create `scaleFontSize(canvasWidth, canvasHeight, ratio)` — returns `Math.round(Math.min(canvasWidth, canvasHeight) * ratio)` for proportional text sizing
2. Create `drawFrostedPill(ctx, x, y, width, height, radius, opacity)` — rounded rectangle with semi-transparent white fill and subtle border
3. Create `drawTextWithHierarchy(ctx, lines, x, y, options)` — draws text block with three-tier hierarchy:
   - Tier 1 (headline): bold, large, fontHeadings
   - Tier 2 (tagline): medium, smaller, fontBody
   - Tier 3 (accent): light, smallest, fontBody
   - Each tier can have optional frosted pill container
</action>
<acceptance_criteria>
- scaleFontSize returns proportional sizes for all platform dimensions
- drawFrostedPill renders rounded rect with semi-transparent fill
- drawTextWithHierarchy renders three-tier text blocks
</acceptance_criteria>
</task>

<task>
<name>Apply typography to Instagram Story</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 228-310)
</read_first>
<action>
1. Replace fixed `bold 58px sans-serif` with proportional fontHeadings
2. Replace fixed `26px sans-serif` tagline with proportional fontBody
3. Wrap tagline in frosted pill (use drawFrostedPill)
4. Add CTA tier (tier 3) with light accent text below tagline
5. Use scaleFontSize: headline 0.055, tagline 0.025, accent 0.018
</action>
<acceptance_criteria>
- Business name uses brand fontHeadings at proportional size
- Tagline in frosted pill with brand fontBody
- Three-tier hierarchy visible (name, tagline, CTA)
</acceptance_criteria>
</task>

<task>
<name>Apply typography to Facebook Cover</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 312-363)
</read_first>
<action>
1. Replace fixed `bold 36px sans-serif` with proportional fontHeadings
2. Replace fixed `17px sans-serif` tagline with proportional fontBody
3. Wrap tagline in frosted pill
4. Add tier 3 accent text (website URL or CTA)
5. Use scaleFontSize with appropriate ratios for 820×312
</action>
<acceptance_criteria>
- Brand fonts applied with proportional sizing
- Frosted pill behind tagline
- Three-tier hierarchy visible
</acceptance_criteria>
</task>

<task>
<name>Apply typography to Twitter Header</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 365-399+)
</read_first>
<action>
1. Replace fixed font sizes with proportional fontHeadings/fontBody
2. Wrap tagline in frosted pill
3. Add tier 3 accent text
4. Use scaleFontSize for 1500×500 dimensions
</action>
<acceptance_criteria>
- Brand fonts applied
- Frosted pill behind tagline
- Three-tier hierarchy
</acceptance_criteria>
</task>

<task>
<name>Apply typography to LinkedIn Banner</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 416-493)
</read_first>
<action>
1. Replace fixed `bold 40px sans-serif` and `20px sans-serif` with proportional brand fonts
2. Wrap tagline in frosted pill
3. Add tier 3 accent text
4. Use scaleFontSize for 1584×396 dimensions
</action>
<acceptance_criteria>
- Brand fonts applied
- Frosted pill behind tagline
- Three-tier hierarchy
</acceptance_criteria>
</task>

<task>
<name>Apply typography to Pinterest Pin</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 495-568)
</read_first>
<action>
1. Replace fixed `bold 50px sans-serif` and `26px sans-serif` with proportional brand fonts
2. Wrap tagline in frosted pill in bottom section
3. Add tier 3 accent text
4. Use scaleFontSize for 1000×1500 dimensions
</action>
<acceptance_criteria>
- Brand fonts applied
- Frosted pill behind tagline
- Three-tier hierarchy
</acceptance_criteria>
</task>

<task>
<name>Apply typography to TikTok Cover</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 570-652)
</read_first>
<action>
1. Replace fixed `bold 66px sans-serif` and `28px sans-serif` with proportional brand fonts
2. Wrap tagline in dark-tinted frosted pill (not white — matches dark theme)
3. Keep neon text shadow on business name
4. Add tier 3 accent text (@handle already exists as tier 3)
5. Use scaleFontSize for 1080×1920 dimensions
</action>
<acceptance_criteria>
- Brand fonts applied with neon shadow
- Dark-tinted frosted pill behind tagline
- Three-tier hierarchy
</acceptance_criteria>
</task>

<task>
<name>Apply typography to Email Header</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (lines 654-700)
</read_first>
<action>
1. Replace fixed `bold 22px sans-serif` and `12px sans-serif` with proportional brand fonts
2. Wrap tagline in frosted pill (conditional: only if tagline < 40 chars and space allows)
3. Use scaleFontSize for 600×200 dimensions (will be small but readable)
4. Simplified treatment — compact template doesn't need full three-tier
</action>
<acceptance_criteria>
- Brand fonts applied at small proportional sizes
- Frosted pill behind tagline (if space allows)
- Clean and minimal for compact size
</acceptance_criteria>
</task>

<task>
<name>Cross-template design consistency audit</name>
<read_first>
- src/hooks/useClientSocialGenerator.ts (full file)
</read_first>
<action>
1. Review all 7 templates side-by-side for design consistency:
   - Same font families used across all templates
   - Same proportional ratios (headline/tagline/accent)
   - Same frosted pill style (opacity, border, radius)
   - Same accent element density (3-4 per template)
   - Same color role assignments (primary=background, secondary=accents, accent=text/highlights)
2. Fix any inconsistencies found
3. Verify no template uses hardcoded pixel font sizes (all should use scaleFontSize)
</action>
<acceptance_criteria>
- All templates use brand fonts consistently
- Proportional sizing ratios consistent across templates
- Frosted pill style uniform
- Accent density consistent (3-4 per template)
- No hardcoded pixel font sizes remain
</acceptance_criteria>
</task>

</tasks>

<success_criteria>
- Brand font loading infrastructure in place (loadBrandFonts, getFont)
- All 7 templates use brand fonts via fontHeadings/fontBody
- Three-tier typography hierarchy (headline, tagline, accent) across all templates
- Frosted glass containers behind tagline text
- Proportional text sizing (no hardcoded pixel sizes)
- Design consistency verified across all 10 platform templates
</success_criteria>

<output>
After completion, create SUMMARY.md with:
- Font loading implementation details
- Typography helper functions created
- Per-template typography changes
- Design consistency audit results
- Commits: List of commits produced
</output>
