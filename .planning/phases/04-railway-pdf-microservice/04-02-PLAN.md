---
phase: 04-railway-pdf-microservice
plan: 04-02
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/lib/services/order-processor.ts
  - .env.local
requirements:
  - SVC-02
  - SVC-03
  - SVC-04
autonomous: true
---

<objective>
Test Puppeteer PDF generation through the full order flow: order creation → logo generation → admin finalization → Puppeteer PDF → ZIP. Verify end-to-end integration, document performance, and assess production readiness on Vercel.

Purpose: Confirm the USE_PUPPETEER_PDF integration in order-processor.ts works correctly and identify any issues before Vercel deployment.
</objective>

<context>
@.planning/phases/04-railway-pdf-microservice/04-CONTEXT.md
@.planning/phases/04-railway-pdf-microservice/04-RESEARCH.md
@.planning/phases/04-railway-pdf-microservice/04-01-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<verification>
Full order flow completes with Puppeteer PDF in the ZIP output. Visual quality meets professional standard.
</verification>

<tasks>

<task>
<name>Review order-processor Puppeteer integration</name>
<read_first>
- src/lib/services/order-processor.ts (lines 214-310)
- src/lib/services/pdf-generator-puppeteer.ts (lines 453-573, generateBrandGuidePDFPuppeteer)
- src/lib/services/pdf-data-transformer.ts
</read_first>
<action>
1. Read order-processor.ts lines 214-310 to understand the Puppeteer integration
2. Verify the flow: USE_PUPPETEER_PDF check → brand assets → data transform → generateBrandGuidePDFPuppeteer → fallback to jsPDF
3. Check for any missing error handling or edge cases:
   - What happens if brand assets fail to load?
   - What happens if template rendering throws?
   - Is the browser properly closed after generation?
4. Document any issues or improvements needed
</action>
<acceptance_criteria>
- Order-processor Puppeteer flow understood and documented
- Error handling verified (try/catch with jsPDF fallback)
- Browser cleanup verified (browser.close() in finally block)
</acceptance_criteria>
</task>

<task>
<name>Fix any integration issues</name>
<read_first>
- src/lib/services/order-processor.ts
- src/lib/services/pdf-generator-puppeteer.ts
</read_first>
<action>
1. Fix any issues found in the review task
2. Ensure browser.close() is always called (even on error)
3. Verify the fallback chain: Puppeteer → jsPDF → throw
4. Add logging for PDF generation timing if missing
</action>
<acceptance_criteria>
- Browser always closed after PDF generation (success or failure)
- PDF generation time logged: `Puppeteer PDF generated in {N}ms`
- Fallback to jsPDF works when Puppeteer fails
</acceptance_criteria>
</task>

<task>
<name>Full order flow test documentation</name>
<read_first>
- src/app/api/orders/[id]/generate/route.ts
- src/app/api/orders/[id]/finalize/route.ts
</read_first>
<action>
1. Document the exact steps to test the full order flow:
   a. Create an order via Stripe checkout (or directly in DB for testing)
   b. Trigger logo generation: `POST /api/orders/{id}/generate`
   c. Admin selects logo variant and finalizes: `POST /api/orders/{id}/finalize`
   d. Verify ZIP contains PDF generated by Puppeteer
   e. Open PDF and visually review all 12 sections
2. Create a test checklist that can be followed manually
3. Note: This test requires a running dev server with database connection
</action>
<acceptance_criteria>
- Test checklist created with numbered steps
- Each step includes expected outcome
- Checklist covers: order creation, generation, finalization, ZIP contents, PDF quality
</acceptance_criteria>
</task>

<task>
<name>Vercel production readiness assessment</name>
<read_first>
- src/lib/services/pdf-generator-puppeteer.ts (lines 421-443, Vercel path)
- package.json (check @sparticuz/chromium version)
</read_first>
<action>
1. Review the Vercel serverless path in launchBrowser():
   - @sparticuz/chromium configuration
   - LD_LIBRARY_PATH setup for Amazon Linux 2
   - Chrome args (--no-sandbox, --disable-setuid-sandbox, etc.)
2. Check package.json for @sparticuz/chromium and puppeteer-core versions
3. Document known Vercel limitations:
   - Function timeout (10s Hobby, 60s Pro)
   - Memory limits (1024MB Hobby, 3008MB Pro)
   - Cold start Chrome download (~50MB)
4. Create a deployment checklist for enabling Puppeteer on Vercel
5. Note: Railway microservice remains optional if Vercel proves reliable
</action>
<acceptance_criteria>
- Vercel path code reviewed and documented
- Package versions noted
- Deployment checklist created
- Limitations documented with mitigation strategies
</acceptance_criteria>
</task>

</tasks>

<success_criteria>
- Order-processor Puppeteer integration reviewed and any issues fixed
- Browser cleanup verified (no leaked Chrome processes)
- Full order flow test documented with checklist
- Vercel production readiness assessed
- Railway remains optional based on Vercel results
</success_criteria>

<output>
After completion, create SUMMARY.md with:
- Integration review findings
- Fixes applied (if any)
- Test checklist summary
- Vercel readiness assessment
- Commits: List of commits produced
</output>
